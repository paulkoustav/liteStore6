import { AddtocartbtnparamsDirective } from './addtocartbtnparams.directive';

describe('AddtocartbtnparamsDirective', () => {
  it('should create an instance', () => {
    const directive = new AddtocartbtnparamsDirective();
    expect(directive).toBeTruthy();
  });
});
